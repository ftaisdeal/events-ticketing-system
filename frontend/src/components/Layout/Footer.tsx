import React from 'react';

const Footer = (): JSX.Element => (
	<footer className="site-footer">
		<div className="site-footer__inner">
			<p>RDX Tickets</p>
			<p>Fast checkout, verified orders, and smooth check-in workflows.</p>
		</div>
	</footer>
);

export default Footer;
